# 🚀 推广操作手册(保姆级步骤)

> 按顺序做,每步 5-15 分钟。做完前 3 步,搜索引擎流量通道全部打开。

---

## 第 1 步:百度搜索资源平台(国内流量大头,优先做)

### 目标
让百度收录 owntools.cn,搜「JSON 在线格式化」「时间戳转换」等词能找到你。

### 具体步骤
1. 浏览器打开 **https://ziyuan.baidu.com**,用百度账号登录
2. 点顶部「**普通收录**」旁的用户头像 →「**站点管理**」→「**添加网站**」
3. 输入 `https://owntools.cn`(协议头选 https),网站类型选「免费工具/其他」
4. 验证方式选第三种「**文件验证**」:
   - 百度会显示一个类似 `baidu_verify_xxxx.html` 的文件名和一段内容
   - 在仓库根目录新建该文件,内容照抄百度给的
   - `git add` + `git push` 推上去,等 2 分钟 Pages 部署完成
   - 回到百度页面点「**完成验证**」
   - ⚠️ 不要选 CNAME 验证,会和 GitHub Pages 的 A 记录冲突
5. 验证成功后,进入「**普通收录**」→「**sitemap**」→ 提交:`https://owntools.cn/sitemap.xml`
6. 再点「**手动提交**」→ 填 `https://owntools.cn/` 提交首页

### 效果预期
3-7 天开始收录,新站沙盒期约 1 个月,之后搜索词逐渐带量。

---

## 第 2 步:Google Search Console(看搜索词报表必配)

### 具体步骤
1. 打开 **https://search.google.com/search-console**,Google 账号登录
2. 点左上角「+ 添加资源」→ 选「**网域**」型(左边那个)→ 输入 `owntools.cn`
3. Google 给你一条 TXT 记录,格式类似:
   `google-site-verification=xxxxxxx`
4. 去阿里云 DNS 控制台(https://dns.console.aliyun.com):
   - 找到域名 `owntools.cn` →「解析设置」→「**添加记录**」
   - 记录类型:`TXT`,主机记录:`@`,记录值:粘贴 Google 给的整串
   - TTL 默认即可,保存
5. 等 5 分钟,回 Google 点「**验证**」
6. 验证成功后:左侧「**站点地图**」→ 输入 `sitemap.xml` → 提交
7. 顶部搜索框(「网址检查」)输入 `https://owntools.cn/` → 点「**请求编入索引**」

### 之后每天看这里
- 「**效果**」报告:哪些搜索词带来多少点击(核心报表)
- 「覆盖率」:收录了多少页面

---

## 第 3 步:Bing 站长平台(顺手,5 分钟)

`BingSiteAuth.xml` 已在仓库根目录,验证大概率已自动通过,只需确认+提交:

1. 打开 **https://www.bing.com/webmasters**,微软账号登录
2. 首页「导入」按钮 → 选「**从 Google Search Console 导入**」(如果第 2 步已做)→ 自动带入全部配置
   - 或手动添加:`owntools.cn`,验证方式选 XML 文件(已部署)
3. 左侧「**站点地图**」→ 提交 `https://owntools.cn/sitemap.xml`
4. 「URL 提交」→ 提交首页

---

## 第 4 步:社区发帖(短期流量最快,复制即用)

### V2EX(开发者浓度最高,先发这个)
1. 打开 **https://www.v2ex.com**,注册/登录(需一定活跃度才能发主题)
2. 顶部「创作新话题」→ 节点选「**分享创造**」
3. 标题粘贴:`[分享创造] 做了个免登录的在线测试工具箱 owntools.cn,纯前端、数据不上传`
4. 正文粘贴(推广文案.md 里有完整版):
   > 做测试/开发经常要格式化 JSON、转时间戳、算哈希,来回切换工具很烦,就把 20 个常用小工具整合到一个页面里。全部纯前端实现,输入的数据不会上传服务器。无广告免登录,打开就用。欢迎体验反馈:https://owntools.cn
5. 发布后 1 小时内回评论,帖子活跃度越高挂得越久

### 掘金(SEO 好,会被百度收录)
1. 打开 **https://juejin.cn**,登录 → 右上角「写文章」
2. 标题:`我为什么把 20 个测试小工具做成了一个纯前端网站(免登录、数据不上传)`
3. 正文写 300-500 字:痛点(JSON 格式化要开 N 个网站)→ 方案(截图 + owntools.cn 链接)→ 工具列表
4. 配 2-3 张网站截图,文末放:`在线体验:https://owntools.cn(纯前端,数据不上传)`
5. 标签选:`前端`、`测试`、`开源`

### 知乎回答(长尾流量,每周做 2 个)
1. 知乎搜「JSON 在线格式化」「在线工具推荐」「程序员常用工具」
2. 找关注人数 1000+ 且有近期回答的问题
3. 认真答 200 字以上,自然带出:「我最近常用 owntools.cn,纯前端实现数据不上传,JSON 格式化报错能定位到行」

---

## 第 5 步:明天检查 HTTPS(一条命令)

```
python enforce_https.py
```
输出 `OK` 即强制 HTTPS 生效,浏览器「不安全」提示消失。

---

## ✅ 效果验收(每天 1 分钟)

| 看什么 | 在哪看 |
|---|---|
| 今日访问/访客 | 网站左下角计数,或 `https://abacus.jasoncameron.dev/hit/halinbx-testgo/pv-20260828`(改日期) |
| 搜索词/点击 | Google Search Console「效果」报告 |
| 收录量 | 百度「索引量」/ GSC「覆盖率」/ Bing「站点地图」 |

## 📅 节奏建议
- **今天**:第 1-3 步(搜索引擎全部配好)
- **本周**:第 4 步发 2 帖(V2EX + 掘金)
- **持续**:知乎每周答 2 题;以后加新工具就重跑 IndexNow(命令在推广文案.md 底部)
